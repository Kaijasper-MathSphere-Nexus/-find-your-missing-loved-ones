requests==2.31.0
pyscript==1.0.0  # Replace with the correct version if known
sqlite3  # Comes pre-installed with Python; no need to specify unless in specific environments
stable-baselines3==1.7.0
sentry-sdk==1.29.2
openai==0.27.8
